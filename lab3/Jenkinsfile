pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                sh 'pwd'
                dir('lab3') {
                    sh 'docker compose up --build'
                }
            }
        }
    }
}