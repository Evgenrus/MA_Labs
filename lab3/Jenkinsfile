pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    sh 'docker compose up --build -f docker-compose.yml'
                }
            }
        }
    }
}